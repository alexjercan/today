interface:
  display_name: "Tatr"
  short_description: "Track Today work in Markdown"
  default_prompt: "Use $tatr to inspect and update Today tracked tasks."
policy:
  allow_implicit_invocation: true
